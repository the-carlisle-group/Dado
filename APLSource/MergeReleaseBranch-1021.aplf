 MergeReleaseBranch←{
     ⍝ ⍵ ←→ ''
     IsDirty'':'Uncommitted changes.'
     b←GetCurrentBranch''
     t v←('/'≠b)⊆b
     'release'≢t:b,' is not a release branch'
     ⍝ Is it tagged???!!!
     ms←(~IsLatestRelease v)/'-s ours '
     ⎕←⊃RunGitCommand'fetch origin master:master'
     ⎕←⊃RunGitCommand'checkout master'
     ⎕←⊃RunGitCommand'merge ',ms,b
     ⎕←⊃DeleteBranch b
     ⎕←⊃PushMaster''
     RefreshProject 0
 }
