 DeleteFolder←{                   ⍝ Delete folder ⍵
     ~⎕NEXISTS ⍵:0                ⍝ No folder, no problem
     cmd←(APLVersion.OS≡'Linux')⊃'rmdir /S /Q ' 'rm -r '
     cmd,←'"',⍵,'"'
     0::1
     r←⎕CMD cmd
     0
 }
