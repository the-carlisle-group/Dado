 GetCommits←{
     ⍝ ⍵ ←→ Specific Branch (defaults to current branch)
     ⍺←0
     p←GetProjectSpace ⍺
     r←p RunGitCommand'log ',⍵,' --pretty=format:"%h∆%an∆%s"'
     m←(⎕CSV⍠'Separator' '∆')⊂r
     s←⎕NS''
     s.(Hash User Message)←↓⍉m
     s
 }
