 NewBranch←{
     ⍝ New Branch from Master
     ⍝ ⍺ ←→ ProjectSpace
     ⍝ ⍵ ←→ Branch
     ⍝ ← ←→ New current branch
     ⍺←0
     p←GetProjectSpace ⍺
     p RunGitCommand'checkout -b ',⍵,' master'
 }
