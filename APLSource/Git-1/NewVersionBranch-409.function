 NewVersionBranch←{
     ⍝ ⍵ ←→ 'x.y.0' [base] ←→ New Version to create
     ⍝ ⍵ ←→ 'x.y.z'        ←→ Existing version to patch
     IsDirty'':'Uncommited changes.'
     v c←2↑(⊆⍵),⊂'master'
     n←GetNumericVersion v
     e←(⊂v)∊(GetTags'').Tag
     (~e)∧0≠2⊃n:'New release must have 0 patch number.'
     r←'.'@(' '∘=)⍕n+0 0 e
     ⎕←RunGitCommand'checkout -b version/',r,' ',e⊃c v
     RefreshProject 0
 }
