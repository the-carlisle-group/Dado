 GenerateReleaseNotes←{
     ⍝ ⍵ ←→
     p←GetCommits''
     r←↑p.Message
     n←1↓p.Message
     t←'# ',r,' Release Notes'
     b←'Version '∘≡¨8↑¨n
     m←(~∨\b)/n
     i←(⍕¨1+⍳≢m),¨⊂'. '
     (⊂t),i,¨m
 }
