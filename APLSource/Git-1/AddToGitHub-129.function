 AddToGitHub←{
     ⍝ ⍵ ←→ GitHub URL
     ⍺←0
     p←GetProjectSpace ⍺
     ⎕←p RunGitCommand'remote add origin ',⍵
     ⎕←p RunGitCommand'remote -v'
     ⎕←p RunGitCommand'push -u origin master'
     'Done'
 }
