 TagRelease←{
     ⍝ ⍵ ←→ ''
     IsDirty'':'Uncommitted changes.'
     b←GetCurrentBranch''
     t v←('/'≠b)⊆b
     'release'≢t:b,' is not a release branch'
     m←'Release ',v
     _←Squash m
     ⎕←⊃RunGitCommand'tag -a ',v,' -m "Version ',v,'"'
     'Done'
 }
