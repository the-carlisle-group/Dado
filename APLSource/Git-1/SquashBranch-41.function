 SquashBranch←{
     ⍝ ⍺ ←→ ProjectSpace
     ⍝ ⍵ ←→ 'New commit message'
     ⍺←0
     p←GetProjectSpace ⍺
     b←GetCurrentBranch p
     k←↑p RunGitCommand'merge-base master ',b
     r←p RunGitCommand'reset ',k
     r,←p RunGitCommand'add -A'
     r,←p RunGitCommand'commit -m"',⍵,'"'
     ⊃r
 }
