 Commit←{
     ⍝ ⍺ ←→ ProjectSpace
     ⍝ ⍵ ←→ Message
     m←⍵,(0=≢⍵)/'Save point'
     ⍺←0
     p←GetProjectSpace ⍺
     r←p RunGitCommand'add --all'
     r,←p RunGitCommand'commit -m "',m,'"'
     ⊃r
 }
