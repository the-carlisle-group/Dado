 IsLatestRelease←{
     ⍝ ⍵ ←→ Version-  'x.y.z'
     ⍝ Assumes ⍵ is already tagged.
     ⍺←0
     p←GetProjectSpace ⍺
     n←GetNumericVersion ⍵
     s←p GetTags''
     n≡↑{⍵[⍒⍵]}s.NumericVersion
 }
