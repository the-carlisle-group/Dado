 IsLatestRelease←{
     ⍝ ⍺ ←→ [Project Folder|Space]
     ⍝ ⍵ ←→ Version-  'x.y.z'
     ⍝ Assumes ⍵ is already tagged.
     ⍺←0
     p←GetProjectFolder ⍺
     n←GetNumericVersion ⍵
     s←p GetTags''
     n≡s.MostRecentVersion
 }
