 GetNextVersion←{
     ⍝ ⍺ ←→ [Project Folder|Space]
     ⍺←0
     p←GetProjectFolder ⍺
     j←⍵,(0=≢⍵)/'patch'
     b←'major' 'minor' 'patch'∊⊂j
     t←p GetTags''
     v←t.MostRecentVersion
     n←v+b×⌽∨\⌽b
     FormatVersion n
 }
