 GetNextVersion←{
     ⍝ ⍺ ←→ [Project Folder|Space]
     ⍺←0
     p←GetProjectFolder ⍺
     b←'major' 'minor' ''∊⊂⍵
     t←p GetTags''
     v←t.MostRecentVersion
     n←v+b×⌽∨\⌽b
     FormatVersion n
 }
