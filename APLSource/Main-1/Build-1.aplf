 Build←{
     ⍝ ⍺ ←→ Project Name
     ⍝ ⍵ ←→ Version
     ⍺←0
     n←GetProjectName ⍺
     p←NameToFQP n
     _←3 ⎕NDELETE p,'dist'
     ⍝ _←p GeneratePackageNotes''
     ⍝ _←p GenerateApplicationNotes''
     ⍝ _←p GenerateReleaseNotes''
     _←⍺ BuildApplication ⍵
     _←⍺ BuildReleaseArtifacts''  ⍝ Should be out of process!
                                  ⍝ Otherwise using
     'Done'
 }
