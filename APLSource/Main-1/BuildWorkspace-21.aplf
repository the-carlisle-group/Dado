 BuildWorkspace←{
     ⍝ ⍺ ←→ Trace
     ⍝ ⍵ ←→ TargetWorkspace, ProjectFolder, Version
     ⍺←0
     tw pf v←⍵
     cl←#.GetCommandLineArgs
     tf←##.AcreConfig.ProjectFolder
     ws←tf,'Assets/Runtime/Build.dws'
     e←0⊃cl
     q←{'"',⍵,'"'}
     a←q ws
     a,←' ProjectFolder=',q pf
     a,←' Version=',v
     a,←' TargetWorkspace=',q tw
     a,←' maxws=2g'
     a,←' Trace=',⍕⍺
     _←RunProcess e a
     0
 }
