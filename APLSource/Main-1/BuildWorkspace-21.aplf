 BuildWorkspace←{
     ⍺←0
     v←⍵
     pf←GetProjectFolder ⍺
     q←{'''',⍵,''''}
     l←⊂']AcreTools.Version'
     ⍝ Once saving from ⎕SE works, we can simply do:
     ⍝ l,←⊂'rc em←',(q pf),' ⎕SE.AcreTools.BuildWorkspace_ ', qv
     l,←⊂'rc em←',(q pf),' (⎕NS ⎕SE.zApps.AcreTools.AcreTools).Main.BuildWorkspace_ ',q v
     l,←⊂'em'
     l,←⊂'⎕OFF (10+rc)'
     ExecuteAPL l
 }
