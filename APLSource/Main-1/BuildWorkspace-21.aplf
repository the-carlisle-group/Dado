 BuildWorkspace←{
     ⍝ ⍺ ←→ Trace
     ⍝ ⍵ ←→ TargetWorkspace Packages
     ⍺←0
     tw p←⍵
     cl←#.GetCommandLineArgs
     tf←##.AcreConfig.ProjectFolder
     ws←tf,'Assets/Runtime/Build.dws'
     e←0⊃cl
     q←{'"',⍵,'"'}
     pl←1↓∊',',¨p
     a←q ws
     a,←' Packages=',q pl
     a,←' TargetWorkspace=',q tw
     a,←' maxws=2g'
     a,←' Trace=',⍕⍺
     _←RunProcess e a
     0
 }
