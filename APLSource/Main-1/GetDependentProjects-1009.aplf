 GetDependentProjects←{
     ⍝ ⍺ ←→ All Projects
     ⍝ ⍵ ←→ Project
     ⍝ ←→ Dependencies, Aliases, AliasTypes
     z←⍬ ⍬ ⍬
     c←⍵.AcreConfig
     0=c.⎕NC'Dependencies':z
     0=≢c.Dependencies:z
     n←⊆c.Dependencies
     n←c.ProjectAuthor∘{'/'∊⍵:⍵ ⋄ ⍺,'/',⍵}¨n ⍝ Do this somewhere else!
     d←⍺[⍺.AcreConfig.FullProjectName⍳n]
     def←{2=c.⎕NC ⍺:c⍎⍺ ⋄ (≢n)⍴⍵}
     a←d.AcreConfig.ProjectName{''≡⍵:⍺ ⋄ ⍵}¨'Aliases'def⊂''
     t←'AliasTypes'def 0
     d a t
 }
