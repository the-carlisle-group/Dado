 RetrieveDependencies←{
     ⍝ ⍺ ←→ Project or Package Name
     ⍝ ⍵ ←→ Current Project State Override
     ⍝ ← ←→ Dependent Names
     ⍺←0
     b←⍵
     pn←GetProjectName ⍺
     f←NameToFQP pn
     c←GetConfigSpace f
     0=c.⎕NC'Dependencies':⍬
     d←c.Dependencies
     0=≢d:⍬
     pa←c.ProjectAuthor
     fqn←pa∘{'/'∊⍵:⍵ ⋄ ⍺,'/',⍵}
     n←fqn¨d.Name
     v←d.Version{⍺ ⍵}¨b
     kn←n RetrievePackage¨v
     kn,↑,/kn ∇¨b
 }
