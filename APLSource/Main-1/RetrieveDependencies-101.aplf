 RetrieveDependencies←{
     ⍝ ⍵ ←→ Project or Package Config Space
     ⍝ ⍺ ←→ Current Project State Override
     ⍝ ← ←→ Dependent project or package config spaces
     0=⍵:⍬
     b←⍺
     0=⍵.⎕NC'Dependencies':⍬
     d←⍵.Dependencies
     0=≢d:⍬
     c←b RetrieveDependency¨d
     c,↑,/b ∇¨c
 }
