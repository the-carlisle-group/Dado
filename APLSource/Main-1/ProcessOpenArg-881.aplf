 ProcessOpenArg←{
     ⍝ Process project path for Environment variables and leading blanks.
     ⍝ ⍵ ←→ Project path
     p←{(∨\' '≠⍵)/⍵}⍵
     b←' -f'≡¯3↑p
     ⎕USING←,⊂⍕'System'
     pp←Environment.ExpandEnvironmentVariables(⊆(b×¯3)↓p)
     pp b
 }
