 SetReferences←{
     ⍝ ⍺ ←→ All project spaces
     ⍝ ⍵ ←→ Project Space
     ⍵.AcreConfig.ProjectName≡'AcreTools':0
     d←⍵.##.##.CarlisleGroup.AcreTools'A' 0
     p a t←d,⍨¨⍺ GetDependentProjects ⍵
     e←GetAPISpace¨p
     r←refs ⍵
     r∘SetReference¨↓⍉⊃a e t
 }
