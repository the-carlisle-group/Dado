 SetReferences←{
     ⍝ ⍺ ←→ All project spaces
     ⍝ ⍵ ←→ Project Space
     ⍵.AcreConfig.ProjectName≡'AcreTools':0
     p a t←⍺ GetDependentProjects ⍵
     0=≢p:0
     e←GetAPISpace¨p
     r←refs ⍵
     r∘SetReference¨↓⍉⊃a e t
 }
