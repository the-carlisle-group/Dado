 ManifestEntry←{
     s←⎕NS''
     s.URL←⍵.URL
     s.Version←⍵.Version
     ⍵.IsPackage:s
     p←⍵.ProjectFolder
     s.Branch←p GetCurrentBranch 0
     s.Hash←p GetShortHash 0
     s.State←(p IsDirty 0)⊃'Clean' 'Dirty'
     s
 }
