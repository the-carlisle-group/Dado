 ExpandProjectPath←{
     ⍝ ⍵ ←→ Character array leading with Project Folder
     f←dlb∘⌽⍣2⊢⍵
     d←GetEnvironmentVariable'DadoProjectsFolder'
     q←(0<≢d)∧~∨/f∊'/\'
     p←f,⍨q/d,'/'
     ExpandPath p
 }
