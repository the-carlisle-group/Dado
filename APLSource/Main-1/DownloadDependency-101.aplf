 DownloadDependency←{
     u f v←⍵
     ⎕NEXISTS f:0 ''
     ⎕←'Downloading ',f,'...'
     c←'clone ',u,' ',f,' --depth 1 --branch ',v
     r←RunProcess'git'c
     c←GetConfigSpace f
     s←c.ProjectSpace,'_','_'@('.'∘=)v
     _←f SetConfigParam'ProjectSpace's
     1(r.StdOut)
 }
