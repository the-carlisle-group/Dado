 ShowCommitDetail←{
     ⍝ ⍺ ←→ [Project Folder|Space]
     ⍺←0
     p←GetProjectFolder ⍺
     w←↑1⊃⎕VFI(⍕⍵),' 1'
     n←(w>0)/'-',⍕w
     r←p RunGitCommand'log ',n,' --name-status'
     ⊃p∘ProcessFileLine¨r
 }
