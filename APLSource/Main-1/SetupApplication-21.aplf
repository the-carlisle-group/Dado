 SetupApplication←{
     ⍝ ⍺ ←→ Library Space
     ⍝ ⍵ ←→ Library Folder
     ⍝ ← ←→ Primary Project Space
     ⍺←#
     c←GetConfigSpace¨⍵∘,¨⍺.Packages
     p←⍺⍎¨2↓¨c.ProjectSpace
     p.AcreConfig←p.⎕NS c
     m t←(¯1↓p)(↑⌽p)
     0=≢m:t⊣GetAPISpace t
     _←m∘SetReferences¨m
     ↑m
 }
