 Retrieve←{
     ⍝ ⍺ ←→ Project Name (e.g. Carlisle/FlipDB)
     ⍝ ⍵ ←→ Version
     ⍝ ← ←→ List of names
     ⍺←0
     n←GetProjectName ⍺
     b←0=≢⍵
     at←'the-carlisle-group/AcreTools'
     av←⎕SE.AcreTools.Version''
     an←'the-carlisle-group/AcreTools'RetrievePackage av 1
     pn←n RetrievePackage ⍵ b
     dn←pn RetrieveDependencies ⍵ b
     (⊂pn),dn,⊂an
 }
