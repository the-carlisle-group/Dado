 Retrieve←{
     ⍝ ⍺ ←→ Project Folder
     ⍝ ⍵ ←→ Version
     ⍝ ← ←→ Config Spaces
     b←0=≢⍵
     _←RetrieveLocalProjects ⍺
     c←GetConfigSpace ⍺
     d←⎕NS''
     d.URL←c.URL
     d.Version←⍵
     pc←b RetrieveDependency d
     dc←b RetrieveDependencies pc
     pc,dc
 }
