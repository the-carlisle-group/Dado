 GetInjectionInfo←{
     ⍝ ⍺ ←→ All Projects
     ⍝ ⍵ ←→ Project
     ⍝ ←→ Dependencies, Aliases, Inject
     z←⍬ ⍬ ⍬
     q c←(⍺ ⍵).AcreConfig
     0=c.⎕NC'Dependencies':z
     0=≢c.Dependencies:z
     m←c.Dependencies
     k←q.(URL Version)⍳m.(URL Version)
     j←q.URL⍳m.URL
     d←⍺[(k=≢q)⊃¨↓⍉⊃k j]
     a←m.Alias
     t←m.Inject
     d a t
 }
