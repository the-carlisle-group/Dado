 SetAppConfig2←{
     ⍝ ⍵ ←→ Application Folder
     ⍝ ← ←→ Primary Project Space
     OBSOLETE
     d←⍵
     r←⍺
     c←GetConfigSpace¨d∘,¨r.Packages,¨'/'
     s←r⍎¨2↓¨c.ProjectSpace
     s.AcreConfig←s.⎕NS c
     ↑s
 }
