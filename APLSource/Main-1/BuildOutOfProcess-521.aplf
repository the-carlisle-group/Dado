 BuildOutOfProcess←{
     ⍺←0
     ⍺/0::⎕OFF 11
     a←+2 ⎕NQ'.' 'GetCommandLineArgs'
     n v←↓⍉⊃{('='≠⍵)⊂⍵}¨a
     get←{(n⍳⊂⍵)⊃v,⊂''}
     tw←get'TargetWorkspace'
     pk←{⍵⊆⍨','≠⍵}get'Packages'
     p←SetupApplication pk
     ⎕LX←SetQuadLX p
     _←⎕EX↑⎕SI
     _←0 ⎕SAVE tw
     ⍺/0::⎕OFF 12
     _←p Execute'OnBuild'
     ⍺:⎕OFF
     0
 }
