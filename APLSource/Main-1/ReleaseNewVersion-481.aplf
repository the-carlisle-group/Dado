 ReleaseNewVersion←{
     ⍝ ⍺ ←→ Project Name
     ⍝ ⍵ ←→ 'major'|'minor'| '' (for patch)
     ⍺←0
     p←GetProjectFolder ⍺
     p≡'':'Not in project'
     v←p CreateNewVersion ⍵
     _←p Build v
     ⎕←⊃p PushMaster''
     _←p ReleaseOnGitHub v
     0
 }
