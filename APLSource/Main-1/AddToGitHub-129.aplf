 AddToGitHub←{
     ⍝ ⍺ ←→ ProjectName
     ⍝ ⍵ ←→ 'n/a
     ⍺←0
     p←GetProjectFolder ⍺
     u←p.AcreConfig.URL,'.git'
     0=≢p:'Not in project'
     _←p EnhanceAcreConfig''
     _←p WriteGitIgnore''
     ⎕←p RunGitCommand'init'
     ⎕←p RunGitCommand'add .'
     ⎕←p RunGitCommand'commit -m ','"~First commit"'
     ⎕←p RunGitCommand'remote add origin ',u
     ⎕←p RunGitCommand'remote -v'
     ⎕←p RunGitCommand'push -u origin master'
     'Done'
 }
