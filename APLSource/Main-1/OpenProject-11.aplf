 OpenProject←{
     ⍝ ⍵ ←→ Project Folder (-f option ←→ force package over existing project)
     pf b←ProcessOpenArg ⍵
     _←RetrieveLocalProjects pf
     c←GetConfigSpace ⍵
     dp←(~b)RetrieveDependencies c
     dp←dp~0                            ⍝ various reasons this could happen.
     n←OpenOrLoad¨(⌽dp),c
     _←SetupWorkspace,⍎¨n
     ↑⌽n

 }
