 GetDependentProjectFolders←{
     ⍝ ⍵ ←→ Primary Project
     ⍝ ← ←→ Dependent Projects, Dependent Packages
     c←GetConfigSpace ⍵
     0=c.⎕NC'Dependencies':⍬ ⍬
     d←c.Dependencies
     0=≢d:⍬ ⍬
     p←⍵⊆⍨~⍵∊'\/'
     pa pn←¯2↑p
     pl←∊(¯2↓p),¨'/'
     fqn←pa∘{'/'∊⍵:⍵ ⋄ ⍺,'/',⍵}
     n←fqn¨d.Name
     v←n ResolveVersion¨d.Version
     pj←pl∘,¨n
     pk←pj{⍺,'-',⍵,'/'}¨v
     pj pk
 }
