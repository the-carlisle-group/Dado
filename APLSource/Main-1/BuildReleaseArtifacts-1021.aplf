 BuildReleaseArtifacts←{
     ⍝ ⍺ ←→ [Project Folder|Space]
     ⍺←0
     n←GetProjectName ⍺
     pf←NameToFQP n
     GetProjectSpace
     c←GetConfigSpace pf
     pn←'OnBuild'
     0=c.⎕NC pn:0

     p⍎c.OnRelease,' 0'
 }
