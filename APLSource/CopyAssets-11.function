 CopyAssets←{
     OBSOLETE
     ⍝ ⍺ ←→ All assets flag
     ⍝ ⍵[0]←→Source Folder (Project folder)
     ⍝ ⍵[1]←→Target Folder
     ⍝ ⍵[2]←→All Assets Flag
     ⍺←0
     aa←⍺
     sf tf←⍵
     _←2 ⎕MKDIR tf
     cs←sf,'acreconfig.txt'       ⍝ Config Source
     ct←tf,'acreconfig.txt'       ⍝ Config target
     _←CopyFile cs ct             ⍝ Copy it


     rs←sf,'Assets/Runtime'
     rt←tf,'Assets/Runtime'
     ~⎕NEXISTS rs:0
     _←⎕MKDIR tf,'Assets'

     _←CopyFolder rs rt
     0
 }
