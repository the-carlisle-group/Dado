 CreateDelivery←{
     ⍝ ⍵ ←→ ProjectSpace
     ⍝ ⍺ ←→ Target Folder

     ⍝ Iterate through the project and its dependent projects/packages
     ⍝ Build packages for each project (if already a package, do nothing
     ⍝ Copy each package to the target folder,


 }
