 SetConfigParam←{
     ⍝ ⍵ ←→ Name and value
     ⍝ ⍺ ←→ Project Space
     ⍺←0
     p←GetProjectSpace ⍺
     n v←⍵
     f←GetProjectFolder p
     ct←f,'acreconfig.txt'
     c←1 ⎕FIX'file://',ct
     _←n c.{⍎⍺,'←⍵'}v
     t←NStoScript c
     _←(⊂t)⎕NPUT ct 1
     0
 }
