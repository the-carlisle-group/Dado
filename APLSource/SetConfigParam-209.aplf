 SetConfigParam←{
     ⍝ ⍵ ←→ Name and value
     ⍝ ⍺ ←→ Project Space
     ⍺←0
     p←GetProjectFolder ⍺
     n v←⍵
     ct←p,'acreconfig.txt'
     c←1 ⎕FIX'file://',ct
     _←n c.{⍎⍺,'←⍵'}v
     t←NStoScript c
     _←(⊂t)⎕NPUT ct 1
     0
 }
